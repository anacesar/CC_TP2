package Gateway;

import java.net.ServerSocket;

import Common.TCPListener;
import Common.UDPListener;

public class HttpGW{
    static int tcp_port = 8080;
    static int udp_port = 8888;

    /* map of conncetions per user */

    //private Map<FastFileServer, Con>

    //private ServerSocket tcp_socket;

    public static void main(){
        /* Creates listening threads for udp and tcp connections */
        //UDPListener udp_listener = new UDPListener();

        while(true){
            
        }
    }
}